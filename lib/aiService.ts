// AI analysis service slot.
// Replace this stub with your preferred backend (Claude, OpenAI, custom endpoint, etc.)
export async function analyzeNote(_text: string): Promise<null> {
  return null;
}
